# Frontend env vars (Vite — prefix VITE_ to expose to browser)
# In dev, Vite proxies /api → localhost:3001 automatically (see vite.config.ts)

VITE_API_URL=/api
# Socket.IO realtime server — in dev connects directly to port 3002
VITE_WS_URL=http://localhost:3002
